.env
.env.*
*.env
credentials.json
*.pem
*.key
.credentials

# Next.js build output (regenerated by Vercel on every deploy)
website/.next/
website/out/

# Node modules (should already be ignored but re-declare for safety)
website/node_modules/
frontend/node_modules/
